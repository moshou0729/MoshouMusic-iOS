name: Build iOS IPA

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Bump version (patch +1) and push back
        # 每次 push 触发的构建都会把 project.yml 里的版本号 patch +1，
        # 这样产物名 MoshouMusic-vX.Y.Z.ipa 与 App 内显示的版本都会随之递增，
        # 真机上能直观区分「更新到了哪个版本」。
        if: github.event_name == 'push'
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          VER=$(grep -oE 'CFBundleShortVersionString: *"[0-9]+\.[0-9]+\.[0-9]+"' project.yml | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          echo "Current version: $VER"
          IFS='.' read -r MAJ MIN PAT <<< "$VER"
          PAT=$((10#$PAT + 1))
          NEW="${MAJ}.${MIN}.${PAT}"
          echo "New version: $NEW"
          sed -i.bak -E 's/CFBundleShortVersionString: "[0-9]+\.[0-9]+\.[0-9]+"/CFBundleShortVersionString: "'"$NEW"'"/' project.yml
          rm -f project.yml.bak
          grep -n "CFBundleShortVersionString" project.yml
          git add project.yml
          git commit -m "chore: bump version to ${NEW} [skip ci]" || echo "no change to commit"
          git push "https://x-access-token:${TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "HEAD:${GITHUB_REF_NAME}" || echo "push failed (version not persisted, IPA still uses bumped value this run)"

      - name: Show environment
        run: |
          sw_vers || true
          xcodebuild -version
          ls /Applications/ | grep -i xcode || true

      - name: Select newest Xcode
        run: |
          ls /Applications/ | grep -E "^Xcode(_[0-9.]+)?\.app"
          NEWEST=$(ls /Applications/ | grep -E "^Xcode(_[0-9.]+)?\.app" | sort -V | tail -1)
          echo "Selecting: $NEWEST"
          sudo xcode-select -s "/Applications/$NEWEST/Contents/Developer"
          xcodebuild -version

      - name: Install XcodeGen (pinned 2.42.0, prebuilt binary, no brew)
        run: |
          set -x
          curl -fsSL -o /tmp/xcodegen.zip https://github.com/yonaskolb/XcodeGen/releases/download/2.42.0/xcodegen.zip
          unzip -q /tmp/xcodegen.zip -d /tmp
          ls -la /tmp/xcodegen/bin/
          echo "/tmp/xcodegen/bin" >> $GITHUB_PATH

      - name: Generate Xcode Project
        run: |
          xcodegen --version
          xcodegen generate
          ls -la MoshouMusic.xcodeproj/
          echo "--- schemes ---"
          xcodebuild -list -project MoshouMusic.xcodeproj

      - name: Build project
        run: |
          set -o pipefail
          xcodebuild \
            -project MoshouMusic.xcodeproj \
            -scheme MoshouMusic \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            -derivedDataPath build 2>&1 | tee build.log | tail -200

      - name: Create IPA
        id: create-ipa
        run: |
          APP_PATH=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name "*.app" | head -1)
          echo "APP_PATH=$APP_PATH"
          echo "--- .app contents ---"
          find "$APP_PATH" -type f | sed "s|$APP_PATH/||" | sort
          if [ ! -d "$APP_PATH/default_scripts" ]; then
            echo "::error::default_scripts folder missing from .app bundle!"
            exit 1
          fi
          # 从 project.yml 读取版本号（CFBundleShortVersionString）
          VERSION=$(grep -oE 'CFBundleShortVersionString: *"[^"]+"' project.yml | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          [ -z "$VERSION" ] && VERSION="dev"
          echo "VERSION=$VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -qry "MoshouMusic-v${VERSION}.ipa" Payload
          ls -la "MoshouMusic-v${VERSION}.ipa"

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: MoshouMusic-${{ steps.create-ipa.outputs.version }}
          path: MoshouMusic-v*.ipa
          retention-days: 30

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            build.log
            build/Logs/**/*.xcactivitylog
          retention-days: 7
          if-no-files-found: ignore
